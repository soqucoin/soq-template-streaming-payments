# Soqucoin streaming micropayments template.
#
# Leave LSP_URL unset to run the LOCAL simulation (no network).
# Set it to the stagenet LSP to stream real payments over a channel.
# LSP_URL=https://lsp.soqu.org
